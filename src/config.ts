/**
 * Configuration schema and loading for opencode-memfs.
 *
 * Reads from ~/.config/opencode/memfs.json with Zod validation.
 * Fields: hotDir, defaultLimit, autoCommitDebounceMs, maxTreeDepth, globalMemoryEnabled.
 */

// TODO: TASK-12 — Define Zod schema for config
// TODO: TASK-12 — Load config from file with defaults
// TODO: TASK-12 — Validate on load, surface errors clearly
