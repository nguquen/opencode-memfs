/**
 * YAML frontmatter parsing, serialization, and atomic file writes.
 *
 * Handles the description/limit/readonly contract.
 * Auto-generates defaults for omitted fields.
 * Uses tmp+rename for atomic writes.
 */

// TODO: TASK-11 — Parse YAML frontmatter from markdown files
// TODO: TASK-11 — Serialize frontmatter with sorted keys
// TODO: TASK-11 — Atomic write (tmp + rename)
// TODO: TASK-11 — Auto-generate defaults (description from filename, limit=5000, readonly=false)
